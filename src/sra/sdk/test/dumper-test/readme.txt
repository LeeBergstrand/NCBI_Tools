(1)
on a linux-shell: ./linux-dump.sh

(2)
in a cygwin-shell on windows: ./win-dump.sh

(3)
on a linux or cygwin-shell: ./compare.sh

what accession is dumped with how many spot-id's
can by fine-tuned inside every dumper-subdir under "dumpout"

every dumper-subdir has a xxx-dump.sh - file
the parameter $1 these files are called with is either "linux" or "win"
